insertOrReplace:
INSERT OR REPLACE
INTO EntryImage
VALUES ?;

selectAll:
SELECT *
FROM EntryImage;

select:
SELECT *
FROM EntryImage
WHERE id = ?;

selectByEntryId:
SELECT *
FROM EntryImage
WHERE entryId = ?;

deleteAll:
DELETE
FROM EntryImage;