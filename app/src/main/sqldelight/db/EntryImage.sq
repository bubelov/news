insertOrReplace:
INSERT OR REPLACE
INTO EntryImage(id, entryId, url, width, height)
VALUES ?;

selectAll:
SELECT *
FROM EntryImage;

select:
SELECT *
FROM EntryImage
WHERE id = ?;

selectByEntryId:
SELECT *
FROM EntryImage
WHERE entryId = ?;

deleteAll:
DELETE
FROM EntryImage;