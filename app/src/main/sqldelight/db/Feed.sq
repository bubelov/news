insertOrReplace:
INSERT OR REPLACE
INTO Feed(id, title, selfLink, alternateLink, openEntriesInBrowser, blockedWords, showPreviewImages)
VALUES ?;

selectAll:
SELECT *
FROM Feed
ORDER BY title;

selectById:
SELECT *
FROM Feed
WHERE id = ?;

deleteAll:
DELETE
FROM Feed;

deleteById:
DELETE
FROM Feed
WHERE id = ?;