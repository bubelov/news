insertOrReplace:
INSERT OR REPLACE
INTO EntryImagesMetadata(entryId, previewImageProcessingStatus, previewImageId, summaryImagesProcessingStatus)
VALUES ?;

selectByEntryId:
SELECT *
FROM EntryImagesMetadata
WHERE entryId = ?;

deleteAll:
DELETE
FROM EntryImagesMetadata;
