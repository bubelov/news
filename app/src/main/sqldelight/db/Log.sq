CREATE TABLE Log (
  id TEXT PRIMARY KEY,
  date TEXT NOT NULL,
  tag TEXT NOT NULL,
  message TEXT NOT NULL,
  stackTrace TEXT
);

insert:
INSERT
INTO Log
VALUES ?;

selectAll:
SELECT *
FROM Log
ORDER BY date DESC;

selectById:
SELECT *
FROM Log
WHERE id = ?;

deleteAll:
DELETE
FROM Log;
