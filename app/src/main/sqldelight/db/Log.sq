insert:
INSERT
INTO Log(date, level, tag, message, stackTrace)
VALUES (?, ?, ?, ?, ?);

selectAll:
SELECT *
FROM Log
ORDER BY date DESC;

selectById:
SELECT *
FROM Log
WHERE id = ?;

selectLastInsertRowId:
SELECT last_insert_rowid();

deleteAll:
DELETE
FROM Log;
